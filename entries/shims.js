import objectAssign    from '../shim/object-assign';
import classListToggle from '../shim/classlist-toggle-force';
import matches         from '../shim/matches';
import closest         from '../shim/closest';
import Event           from '../shim/event';
import CustomEvent     from '../shim/custom-event';

export default {
    objectAssign,
    classListToggle,
    matches,
    closest,
    Event,
    CustomEvent
}